using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using TMPro;
public class AlphaCreate : MonoBehaviour
{
    public TextMeshProUGUI[,] word = new TextMeshProUGUI[8,8];
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
